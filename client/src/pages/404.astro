---
// Mirrors the old SPA catch-all (`<Route path="*" element={<Home />} />`).
import Layout from "../layouts/Layout.astro";
import Home from "../views/Home";
---

<Layout
  title="Page not found — Damn"
  description="That page doesn't exist. Here's the Damn home page instead."
>
  <Home client:load />
</Layout>
